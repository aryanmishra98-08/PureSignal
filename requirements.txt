# =============================================================================
# requirements.txt — PureSignal project
# Python 3.10+ required
# Install: pip install -r requirements.txt
# For exact reproduction: pip install -r requirements-lock.txt
# =============================================================================

# Audio capture and playback
sounddevice>=0.4.6

# WebSocket client for Fixie.ai Ultravox
websockets>=12.0

# Signal processing — resampling, WAV I/O
scipy>=1.11.0

# Numerical computing
numpy>=1.26.0

# Deep learning — MPS backend for Apple Silicon
torch>=2.2.0,<2.4.0

# Speaker embedding model + inference wrapper
pyannote.audio>=3.1.0,<4.0.0

# HuggingFace model loading
transformers>=4.40.0
huggingface-hub>=0.22.0

# Environment variable loading from .env files
python-dotenv>=1.0.0

# YAML config loading
pyyaml>=6.0

# Evaluation plots (optional — skip if running headless)
matplotlib>=3.8.0

# VAD TextGrid label support (optional)
textgrid>=1.5.0
requests>=2.31.0

# Target speaker extraction — new in v2
asteroid>=0.6.0
